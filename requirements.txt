# ── Core UI ───────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.18.0
pandas>=2.1.0
numpy>=1.26.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
# pyodbc>=5.1.0            # Uncomment ONLY for local Windows + SQL Server.
                           # Streamlit Cloud / Heroku / Render don't have the
                           # ODBC driver so this would fail at runtime. The app
                           # auto-falls-back to SQLite when pyodbc is missing.

# ── AI — Local + Cloud ────────────────────────────────────────────────────────
ollama>=0.3.0
groq>=0.4.0

# ── AI — LangGraph + LangChain ───────────────────────────────────────────────
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0

# ── Vector DB ─────────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── FastAPI Backend ───────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-multipart>=0.0.9

# ── Auth & Security ───────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.0
pyotp>=2.9.0               # TOTP (Google Authenticator)
qrcode[pil]>=7.4.2         # QR code for TOTP setup
Pillow>=10.0.0             # Required by qrcode

# ── SMS OTP ───────────────────────────────────────────────────────────────────
twilio>=8.5.0              # SMS OTP delivery

# ── Background Scheduling ─────────────────────────────────────────────────────
apscheduler>=3.10.0

# ── Billing ───────────────────────────────────────────────────────────────────
stripe>=7.0.0

# ── Scraping ──────────────────────────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
playwright>=1.44.0         # headless browser for JS-rendered pages (optional)

# ── Email & Config ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Data / Analytics ─────────────────────────────────────────────────────────
pydantic>=2.5.0
