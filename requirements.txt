# ── Core UI ───────────────────────────────────────────────────────────────────
streamlit>=1.35.0
plotly>=5.18.0
pandas>=2.1.0
numpy>=1.26.0

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy>=2.0.0
alembic>=1.13.0            # Schema migrations — replaces create_all() in prod
psycopg2-binary>=2.9.0     # PostgreSQL driver (Neon / Supabase / Render / etc.)
                           # Activated via DATABASE_URL=postgresql://...
pymssql>=2.3.0             # SQL Server driver for Linux/Cloud.
                           # Activated via AZURE_SQL_* env vars.
                           # Needs `freetds-dev` in packages.txt.
# pyodbc>=5.1.0            # Uncomment ONLY for local Windows + Trusted Connection
                           # to your laptop's SQL Server. Streamlit Cloud doesn't
                           # have the Microsoft ODBC driver.

# ── AI — Local + Cloud ────────────────────────────────────────────────────────
ollama>=0.3.0
groq>=0.4.0
openai>=1.30.0
anthropic>=0.34.0

# ── AI — LangGraph + LangChain ───────────────────────────────────────────────
langgraph>=0.2.0
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
# Durable LangGraph checkpoints — without one of these, HITL workflow state is
# lost on restart. Pick the one matching your prod DB; both installed is fine.
langgraph-checkpoint-sqlite>=2.0.0
langgraph-checkpoint-postgres>=2.0.0

# ── Vector DB ─────────────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── FastAPI Backend ───────────────────────────────────────────────────────────
fastapi>=0.110.0,<0.116
# Starlette 1.0+ dropped Router(on_startup=…) which FastAPI ≤0.115 still passes.
# Pinning until we upgrade to a FastAPI release that uses lifespan handlers.
starlette>=0.27,<0.40
uvicorn[standard]>=0.29.0
httpx>=0.27.0
python-multipart>=0.0.9

# ── Auth & Security ───────────────────────────────────────────────────────────
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt>=4.1.0
pyotp>=2.9.0               # TOTP (Google Authenticator)
qrcode[pil]>=7.4.2         # QR code for TOTP setup
Pillow>=10.0.0             # Required by qrcode

# ── SMS OTP ───────────────────────────────────────────────────────────────────
twilio>=8.5.0              # SMS OTP delivery

# ── Background Scheduling ─────────────────────────────────────────────────────
apscheduler>=3.10.0

# ── Billing ───────────────────────────────────────────────────────────────────
stripe>=7.0.0

# ── Scraping ──────────────────────────────────────────────────────────────────
requests>=2.31.0
beautifulsoup4>=4.12.0
lxml>=5.1.0
playwright>=1.44.0         # headless browser for JS-rendered pages (optional)

# ── Email & Config ────────────────────────────────────────────────────────────
python-dotenv>=1.0.0

# ── Data / Analytics ─────────────────────────────────────────────────────────
pydantic>=2.5.0
